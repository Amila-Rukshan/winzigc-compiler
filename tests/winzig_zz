{ This program tests simple function calls for many argument. }
program MyProg:

var 
    i, j : integer; 
    b, k : boolean;

begin
    i := 15 - 3 * i;
    j := i + 1;
    output(i);
    output(j);
end 

MyProg.
