{ This program tests simple function calls for many argument. }
program MyProg:

var 
    i, j : integer; 
    b, k : boolean;

begin
    i := (5 + 3) * 4;
    j := 10 mod 3 * 5;
    output(i);
    output(j);
end 

MyProg.
