{ This program tests simple function calls for many argument. }
program MyProg:

var 
    i, j : integer; 
    b, k : boolean;

begin
    read(i);
    read(j);
    output(i mod j <> 4);
    i := 100 - i;
    j := 2 * i + j;
    output(i, j);
end

MyProg.
